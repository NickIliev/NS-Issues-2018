
<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:lv="nativescript-pro-ui/listview" navigatingTo="navigatingTo" class="page">

    <Page.actionBar>
        <ActionBar title="My App" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>

    <GridLayout rows="*" columns="*">
        <!-- <ListView items="{{ items }}" >
            <ListView.itemTemplate>
                <Button text="Button" tap="onTap" class="btn btn-primary btn-active"/>
            </ListView.itemTemplate>
        </ListView> -->
        <lv:RadListView items="{{ items }}" >
            <lv:RadListView.itemTemplate>
                    <Button text="Button" tap="onTap" class="play"/>
            </lv:RadListView.itemTemplate>
        </lv:RadListView>
    </GridLayout>
</Page>
